<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mycalculate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity
            android:name=".rotate_calculate.RotateCalculateActivity"
            android:exported="false"
            android:label="교대 정산 화면"/>
        <activity
            android:name=".close_calculate.CalculateActivity"
            android:exported="false"
            android:label="마감 정산 화면" />
        <activity
            android:name=".close.CloseActivity"
            android:exported="false"
            android:label="마감 화면" />
        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>